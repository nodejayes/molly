stages:
  - publish

publish:
  stage: publish
  only:
    - release
  script:
    - yarn install
    - yarn build
    - yarn test
    - sonar-scanner -Dsonar.inclusions=src/**/* -Dsonar.typescript.lcov.reportPaths=coverage/lcov.info
    - npm publish
