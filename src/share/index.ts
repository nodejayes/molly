import {BaseTypes as BT} from './base.types';
import {CollectionInformation as CI} from './collection.information.model';
import {MollyConfiguration as MC} from './molly.configuration.model';
import {ValidationInformation as VI} from './validation.information.model';

export namespace Share {
    export let BaseTypes = BT;
    export let CollectionInformation = CI;
    export let MollyConfiguration = MC;
    export let ValidationInformation = VI;
}